# Core ML and data processing
xgboost>=2.0.0
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0

# Database backends
clickhouse-connect>=0.6.0
minio>=7.2.0
pyarrow>=14.0.0  # For Parquet format

# Monitoring
psutil>=5.9.0

# Model persistence
joblib>=1.3.0

# Optional: Progress bars
tqdm>=4.66.0

# Semantic Features - Required for SemanticXGBoostRouter
transformers>=4.30.0  # For DistilBERT semantic embeddings
torch>=2.0.0          # Deep learning backend for transformers

# Blockchain Integration - Required for Polygon Layer 2 audit trail
web3>=6.0.0           # Ethereum/Polygon blockchain interaction
eth-account>=0.9.0    # Ethereum account management and signing
hexbytes>=0.3.0       # Hex-encoded byte string utilities

# Note: For CPU-only (lighter install):
# pip install torch --index-url https://download.pytorch.org/whl/cpu
